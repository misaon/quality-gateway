/**
 * `quality-gateway init` — detect the project's stack, install the matching
 * `@misaon/eslint-config-*`, and write `eslint.config.js`. Not implemented yet.
 */
export async function runInit(): Promise<void> {
  console.log('quality-gateway init — not implemented yet.')
}
